version: "3.9"
services:
  registry:
   image: registry:2
   container_name: registry
   restart: always
   ports: ["5001:5000"]
   volumes: [registry-data:/var/lib/registry]
volumes:

  registry-data: